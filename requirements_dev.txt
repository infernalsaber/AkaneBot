# These requirements are in addition to requirements.txt

pyupgrade==3.10.1
ruff==0.0.285
seed-isort-config # Probably won't need this in a venv
isort==5.12.0
black==23.7.0
pre-commit==3.3.3
ipdb

## The following aren't needed per-se, but might be helpful if you want to develop on this
# no_implicit_optional
# mypy